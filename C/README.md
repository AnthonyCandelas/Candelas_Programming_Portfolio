C starter notes

- Copy Makefile.template into the assignment folder and rename it to makefile.
- Set TARGET to the binary name you want (default is main).
- Keep your .c files in that folder; run make, make run, and make clean as needed.
- Add libraries to LDFLAGS if an assignment needs them.
